# Python（build_data.py 运行产物缓存）
__pycache__/
*.py[cod]

# 编辑器 / IDE
.vscode/
.idea/
*.swp
*.swo

# 系统文件
Thumbs.db
Desktop.ini
.DS_Store

# 本地 agent 工作区配置与私有文档（不入库）
.trae/
DEPLOY.local.md
# 若日后确需新增仅本机保留的文档，用 *.local.md 命名（docs/ 下三份运维文档已改为入库）

# 构建产物：index.json 需要提交（Pages 无构建时可作为数据源）；
# downloads/*.zip 由 Netlify build 或本地 `python build_data.py` 生成，不入库。
downloads/

# 首屏画廊生成器的本地缓存（抠图结果；口径与源图哈希做键，可随时删，不入库）
gallery/.cache/

# 本地图片优化镜像（未接入构建，避免误提交）
web-assets.local/

# 本地溯源目录（保留一份素材缓存供溯源，不参与提交）
_溯源/

# 本地检查脚本（ZIP 自包含性、链接可达性等一次性校验）
_*.py
_*.mjs

# 素材投放区（临时收图用，入库前先归入 char/<slug>/assets/）
未分类的素材/

# 记忆服务的项目层状态（会话产物，不入库）
.dsh/

# 人工验证清单（本机待办板，不入库）
human.md
